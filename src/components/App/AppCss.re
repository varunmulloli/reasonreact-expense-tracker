open Css;

let container = style([
  width(px(768)),
  marginTop(Theme.marginWidth),
  marginRight(`auto),
  marginBottom(Theme.marginWidth),
  marginLeft(`auto),
]);

let contents = style([
  marginTop(Theme.marginWidth),
]);