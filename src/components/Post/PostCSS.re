open Css;

let postContainer = style([
  width(px(600)),
  marginLeft(`auto),
  marginRight(`auto),
  marginTop(px(16)),
  padding(px(16)),
  border(px(1),`solid,hex("000")),
]);

let postTitle = style([
  fontWeight(`bold),
  fontSize(px(18)),
  marginBottom(px(4)),
]);

let postBody = style([

]);